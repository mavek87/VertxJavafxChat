mainClassName = 'com.matteoveroni.vertxjavafxchatclient.ClientApp'

if (!hasProperty('mainClass')) {
    ext.mainClass = mainClassName
}

dependencies {
    compile(project(":VertxJavafxChatBusinessLogic"))
}